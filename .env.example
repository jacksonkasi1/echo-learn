# ===========================================
# Echo-Learn Environment Variables
# ===========================================
# Updated: Now using Gemini 2.0 Flash for OCR (replacing Mistral)

# ===========================================
# REQUIRED: Upstash Vector (for embeddings storage)
# ===========================================
UPSTASH_VECTOR_REST_URL=
UPSTASH_VECTOR_REST_TOKEN=

# ===========================================
# REQUIRED: Upstash Redis (for caching and state)
# ===========================================
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# ===========================================
# REQUIRED: Google Cloud Storage
# ===========================================
GCS_BUCKET_NAME=
GCS_PROJECT_ID=
GCS_KEY_FILE=              # Path to service account JSON key file
GCS_PUBLIC_URL=            # Public URL for file access (optional)

# ===========================================
# REQUIRED: Gemini AI (LLM, OCR, and Embeddings)
# ===========================================
# Single API key for all AI operations:
# - Text generation (Gemini 2.0 Flash)
# - Multimodal OCR (PDF, images, documents)
# - Text embeddings
GOOGLE_GENERATIVE_AI_API_KEY=
GEMINI_MODEL=gemini-2.0-flash

# ===========================================
# REQUIRED: 11Labs Voice AI
# ===========================================
ELEVENLABS_API_KEY=
ELEVENLABS_AGENT_ID=
ELEVENLABS_AGENT_SECRET=    # Used for verifying Custom LLM requests

# ===========================================
# OPTIONAL: Legacy Mistral OCR (fallback only)
# ===========================================
# Gemini 2.0 Flash is now the primary OCR solution.
# Mistral is kept as an optional fallback.
MISTRAL_API_KEY=

# ===========================================
# Server Configuration
# ===========================================
PORT=8787
NODE_ENV=development

# ===========================================
# Logging
# ===========================================
LOG_LEVEL=info              # debug | info | warn | error

# ===========================================
# CORS (comma-separated origins)
# ===========================================
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# ===========================================
# Authentication (optional, for production)
# ===========================================
API_SECRET_KEY=             # Secret key for API authentication
