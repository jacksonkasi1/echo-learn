# ===========================================
# Echo-Learn Server - Environment Configuration
# ===========================================
# Copy this file to .env and fill in your actual values
# DO NOT commit sensitive keys to version control

# ===========================================
# Server Configuration
# ===========================================
# Port the server will listen on
PORT=8787

# Node environment (development, production, test)
NODE_ENV=production

# CORS allowed origins (comma-separated)
# For Cloud Run, typically use the actual domain
CORS_ORIGINS=https://yourdomain.com,https://app.yourdomain.com

# ===========================================
# Upstash Vector Database
# ===========================================
# REST API endpoint for vector database
# Get from: https://console.upstash.com/vector
UPSTASH_VECTOR_REST_URL=https://your-endpoint-us1-vector.upstash.io

# Authentication token for vector database
# Keep this secret! Store in Cloud Run secrets, not in code
UPSTASH_VECTOR_REST_TOKEN=ABUFYourTokenHereKeepItSecret1234567890

# ===========================================
# Upstash Redis Cache
# ===========================================
# REST API endpoint for Redis cache
# Get from: https://console.upstash.com/redis
UPSTASH_REDIS_REST_URL=https://your-endpoint.upstash.io

# Authentication token for Redis
# Keep this secret!
UPSTASH_REDIS_REST_TOKEN=AZW7AAYourRedisTokenHereKeepItSecret1234567890

# ===========================================
# Google Cloud Storage (GCS)
# ===========================================
# GCP project ID where your bucket is located
GCS_PROJECT_ID=your-gcp-project-id

# Path to service account JSON file (relative to server root)
# This file should be in .gitignore
GCS_KEY_FILE=gcp-service-account.json

# GCS bucket name for file uploads
GCS_BUCKET_NAME=your-bucket-name

# ===========================================
# Google Generative AI (Gemini)
# ===========================================
# API key for Google's Generative AI service
# Get from: https://aistudio.google.com/apikey
# Keep this secret!
GOOGLE_GENERATIVE_AI_API_KEY=AIzaSyYourGeminiAPIKeyHere1234567890abcdef

# ===========================================
# Cohere AI
# ===========================================
# API key for Cohere language models
# Get from: https://dashboard.cohere.com/api-keys
# Keep this secret!
COHERE_API_KEY=your-cohere-api-key-keep-it-secret

# ===========================================
# Mistral AI (OCR)
# ===========================================
# API key for Mistral's OCR and vision capabilities
# Get from: https://console.mistral.ai/
# Keep this secret!
MISTRAL_API_KEY=your-mistral-api-key-keep-it-secret

# ===========================================
# ElevenLabs (Voice/Audio)
# ===========================================
# API key for ElevenLabs text-to-speech service
# Get from: https://elevenlabs.io/app/api-keys
# Keep this secret!
ELEVENLABS_API_KEY=sk_your_elevenlabs_key_here_keep_it_secret

# Agent ID for the conversational agent
# Get from: https://elevenlabs.io/app/agents
ELEVENLABS_AGENT_ID=agent_your_agent_id_here

# Agent secret for authentication
# Keep this secret!
ELEVENLABS_AGENT_SECRET=your-agent-secret-keep-it-secret

# ===========================================
# Production Deployment Notes
# ===========================================
# For Cloud Run deployment:
# 1. Store all secrets in Cloud Run > Secret Manager
# 2. Reference them in Cloud Run environment variables
# 3. Never commit actual secrets to git
# 4. Use environment-specific files: .env.prod, .env.beta, .env.sandbox
# 5. The deploy.sh script will read from .env.$ENV_NAME
#
# Example: To deploy to production:
#   GCP_REGION=asia-southeast1 GCP_SA_KEY_PATH=./gcp-sa.json ./scripts/deploy.sh prod
